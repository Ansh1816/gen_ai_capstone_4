# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environment
venv/
env/
.env

# ChromaDB — vector store is committed so the hosted app ships with an
# already-populated knowledge base (otherwise the RAG tool returns empty
# results in production and the agent hallucinates policies).
# If you change the guideline markdown files, re-run build_vectorstore.py
# and commit the refreshed chroma_db/ directory.
# chroma_db/

# macOS
.DS_Store

# Large data file (optional — remove line below if you want it tracked)
KaggleV2-May-2016.csv

# Streamlit cache
.streamlit/

# Jupyter
.ipynb_checkpoints/
